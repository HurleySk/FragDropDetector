# Reddit API Credentials (REQUIRED)
# Get these from https://www.reddit.com/prefs/apps
REDDIT_CLIENT_ID=your_client_id_here
REDDIT_CLIENT_SECRET=your_client_secret_here
REDDIT_USER_AGENT=FragDropDetector/1.0 (by /u/YourUsername)

# Subreddit Configuration
SUBREDDIT=MontagneParfums
CHECK_INTERVAL=300  # Check every 5 minutes
POST_LIMIT=50  # Number of posts to check

# Firebase Cloud Messaging Notifications
# Create a Firebase project and download service account JSON
# Subscribe to 'fragdrops' topic in your app to get alerts
FCM_SERVICE_ACCOUNT=path/to/firebase-service-account.json
FCM_TOPIC=fragdrops  # Topic name for subscribers
# Alternative: Set GOOGLE_APPLICATION_CREDENTIALS environment variable

# Email Notifications (Optional)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
EMAIL_SENDER=your_email@gmail.com
EMAIL_PASSWORD=your_app_password_here
EMAIL_RECIPIENTS=recipient1@email.com,recipient2@email.com

# Testing
SEND_TEST_NOTIFICATION=false  # Set to true to send test notification on startup